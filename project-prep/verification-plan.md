# AI Bestie Verification Plan

Use this loop before stage completion:

- L1 static checks: typecheck, lint, unit
- L2 component checks: routes/components/config wiring + accessibility basics
- L3 integration checks: auth/db/ai/integration correctness
- L4 browser/system checks: end-to-end user path validation
- Docs freshness checks
- Git hygiene checks

Canonical details: `project-prep/ai-bestie-preparation-bundle.md` sections 8 and 10.
